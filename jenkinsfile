pipeline {
    agent any

    stages {
        stage('Clean') {
            steps {
                cleanWs()
            }
        }

        stage('Clone Code') {
            steps {
                git branch: 'main', url: 'https://github.com/agrimtech-dev/test-cicd.git'
            }
        }

        stage('Build') {
            steps {
                echo "Build started..."
            }
        }

        stage('Deploy') {
            steps {
                sh '''
                mkdir -p /var/jenkins_home/project
                rm -rf /var/jenkins_home/project/*
                cp -r * /var/jenkins_home/project/
                '''
            }
        }
    }
}
